---
trigger: always_on
---

- always use pnpm instal of npm. If `pnpm` is not found, prepend your commands with `export NVM_DIR="$HOME/.nvm" && [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh" &&` to ensure the environment is correctly loaded.
- update docs regularly for any changes you make in the codebase
- dont assume anything and this is the free space you can exprement explore anything to give me the best result. it is not a production code but i expect performace more
- if you find anything like a workflow or something that took you more searches and tools called update it in this workspace rules, workflow or docs for your future referance **dont not include unwanted things there**
- focus on user experance and smoothness
- backend docs folder - backend/docs
- **I need a robust code with complete optimization on performance and mainly it should be readable and without code smell**